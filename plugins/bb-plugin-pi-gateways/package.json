{
  "name": "bb-plugin-pi-gateways",
  "version": "0.1.0",
  "type": "module",
  "description": "Free OpenCode Zen and Kilo Code models inside bb through pi, by token, without running their CLIs.",
  "engines": {
    "bb": ">=0.39",
    "bbPluginSdk": ">=0.4.8"
  },
  "bb": {
    "name": "Pi Gateways",
    "description": "Adds OpenCode Zen and Kilo Code free models to the pi provider over their HTTP APIs, so neither CLI has to run.",
    "branding": {
      "icon": "./icons/gateway.svg"
    },
    "server": "./server.ts",
    "host": "./host.ts"
  },
  "dependencies": {
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@get-bb/plugin-sdk": "0.4.8",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
